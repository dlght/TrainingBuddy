import { Screen } from "@/components/Screen";

export default function ProfileSetupScreen() {
  return (
    <Screen
      eyebrow="Profile"
      title="Create your profile"
      body="This placeholder will collect name, bodyweight, unit, experience level, and goals."
    />
  );
}
